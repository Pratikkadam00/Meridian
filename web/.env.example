# Meridian website — environment template.
# The anon key is public by design (RLS is the boundary). The service-role key is
# NEVER used here and must never be added to this file.

# Public Supabase project (same project as the app, reused for the waitlist).
NEXT_PUBLIC_SUPABASE_URL=https://ebmsbtnyxymajyywuhyd.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: cookieless analytics domain (Plausible). Leave empty to disable.
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=

# Canonical site URL (used for metadata / sitemap / OpenGraph).
NEXT_PUBLIC_SITE_URL=https://meridian.ae
