Sticky top chrome. Back chevron mirrors in RTL; eyebrow + title; trailing IconButtons.

```jsx
<AppHeader eyebrow="Marina Vista" title="Unit 1204" onBack={goBack}
  actions={<IconButton icon={icon('more-horizontal')} label="More" />} bordered />
```
