AI confidence for an extracted field. Word + distinct shape + color — never a raw %.

```jsx
<ConfidenceCue level="high" />
<ConfidenceCue level="low" chip />   {/* "Low — please verify" treatment */}
```

Levels: `high` (filled check / jade) · `med` (half dot / amber) · `low` (hollow alert / terracotta).
