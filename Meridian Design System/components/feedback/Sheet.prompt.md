Bottom sheet for quick in-context actions. Renders a scrim + handle; mount inside a `position:relative` phone frame.

```jsx
<Sheet open={open} title="Mark as paid" subtitle="DLD 4% fee · AED 75,000" onClose={close}>
  …confirm content…
</Sheet>
```
