Construction % (amber) or paid-to-date % (jade). The numeric label is mandatory — value never depends on bar length/color alone.

```jsx
<ProgressMeter variant="construction" label="Construction" value={42} marker={60} />
<ProgressMeter variant="payment" label="Paid to date" value={35} />
```
